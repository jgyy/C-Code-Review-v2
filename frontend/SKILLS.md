<!-- C-Code-Review frontend/, SKILLS.md. -->

# Skills: frontend/

| Task | Where |
|---|---|
| Add a new route | `app/` — see `app/SKILLS.md` |
| Add or change a UI component | `components/` — see `components/SKILLS.md` |
| Call a new backend endpoint | `lib/api.ts`'s `request()` helper — see `lib/SKILLS.md` |
| Change route protection / auth redirect | `middleware.ts` |
| Run the dev server | `pnpm dev` |
