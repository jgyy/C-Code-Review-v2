<!-- C-Code-Review frontend/app/, SKILLS.md. -->

# Skills: frontend/app/

| Task | Where |
|---|---|
| Add a new authenticated page | `(app)/` — not top-level |
| Change the sign-in / OAuth flow | `(auth)/` |
| Fetch data on a page | via `../lib/api.ts`, never a backend URL constructed inline |
